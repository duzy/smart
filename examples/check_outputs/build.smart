project

check: \
  check-status \
  check-stdout \
  check-stderr
	println done "$@ ($^)"

check-status:[(shell -oe -s) (check status=123)]
	exit 123

check-stdout:[(shell) (check stdout='okay')]
	echo -n "okay"

check-stderr:[(shell) (check stderr='okay')]
	echo -n "okay" > /dev/stderr
