module c++

#extensions cpp cc CC C hh h o
extensions (
  unit=( cpp cc CC C )
  header=( hh h )
  object=( o obj )
)

link_flags =
libs = 

linker = g++
link = $(linker) $(link_flags)

compiler = g++
compile = $(compiler)

sources =

compile-sources:
	println 'TODO: compile $(sources)'
