project testcontains

val := a b c $1

# eval -ddd=builtin
.test.1 := $(contains a,$(val))
.test.2 := $(contains c,$(val))
.test.3 := $(contains x,$(val))
# eval -ddd=none{}

foo: ; $(val $@)

.test.4 := $(contains foo,${foo})
