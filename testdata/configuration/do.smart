# NOTE: outtmp must be set before configuration; -configure loads <.smart/modules>/configure by default
project -configure -set(variant=darwin/arm64/bootstrap) testdefaultconfigure

configure FOO : foo ; $(.self)

foo:
