# NOTE: outtmp must be set before configuration;
# NOTE: -configure loads [.smart/modules]/configure by default
project -configure -set(variant=darwin/arm64/bootstrap) testdefaultconfigure

FOO : {(configure)} ; $(.self)
